- error = @recipe.errors.include?(:title)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :title, :class => 'control-label'
  .controls
    =f.text_field :title, :class => 'form-control input-large input-with-feedback tooltip-success', 
      :autofocus => true,
      :'data-toggle'=>"tooltip",:'data-placement'=>'top', :title=>'Title for the recipe'
    %span.help-inline=error ? f.error_message_on(:title, :class => 'text-error') : pat(:example)

- error = @recipe.errors.include?(:content)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :content, :class => 'control-label'
  .controls
    ~f.text_area :content, :class => 'form-control input-large input-with-feedback tinymce'
    %span.help-inline=error ? f.error_message_on(:body, :class => 'text-error') : pat(:example)

- error = @recipe.errors.include?(:main_ingredient)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :main_ingredients, :class => 'control-label'
  .controls
    =f.text_area :main_ingredient, :class => 'form-control input-large input-with-feedback', :autofocus => true
    %span.help-inline=error ? f.error_message_on(:title, :class => 'text-error') : pat(:example)

- error = @recipe.errors.include?(:image)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :image, :class => 'control-label'
  .controls
    ~f.file_field :image, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:image, :class => 'text-error') : pat(:example)

- error = @recipe.errors.include?(:prep_time)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :prep_time, :class => 'control-label'
  .controls
    =f.text_field :prep_time, :class => 'form-control input-large input-with-feedback', :autofocus => true
    %span.help-inline=error ? f.error_message_on(:prep_time, :class => 'text-error') : pat(:example)

- error = @recipe.errors.include?(:cooking_time)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :cooking_time, :class => 'control-label'
  .controls
    =f.text_field :cooking_time, :class => 'form-control input-large input-with-feedback', :autofocus => true
    %span.help-inline=error ? f.error_message_on(:cooking_time, :class => 'text-error') : pat(:example)
    
- error = @recipe.errors.include?(:course)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :course, :class => 'control-label'
  .controls
    =f.select :course, :options =>['breakfast','brunch''lunch','dinner', 'night'] ,:class=>'form-control'
    %span.help-inline=error ? f.error_message_on(course, :class => 'text-error') : pat(:example)
  
  .form-actions
  =f.submit pat(:save), :class => 'btn btn-primary'
  &nbsp;
  =f.submit pat(:save_and_continue), :class => 'btn btn-info', :name => 'save_and_continue'
  &nbsp;
  =link_to pat(:cancel), url(:recipes , :index), :class => 'btn btn-default'
